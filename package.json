{
  "name": "dj-presskit-template",
  "version": "1.0.0",
  "private": true,
  "description": "Presskit DJ : site statique piloté par un fichier JSON, plus une version document en PDF.",
  "scripts": {
    "build": "node build/site.mjs site/content.json dist/site/index.html && node build/site.mjs site/content.example.json dist/site/exemple.html && node build/brand.mjs dist/site/brand.html && node build/artiste.mjs site/content.json dist/site/artistes/a-res.html",
    "dev": "npm run build && node build/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
